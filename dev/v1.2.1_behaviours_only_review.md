# v1.2.1 behaviour-pack review status

This file records the current state of the v1.2.1 behaviour-pack audit. It is a static decision/code-audit summary, not proof that every feature has been fully gameplay-tested in Minecraft.

## Plain-English status

- **Audit decisions:** complete. There are no remaining behaviour-pack files in this audit that still need a port/retain decision.
- **Code/static recheck:** complete for the previously listed non-structure items. The remaining byte differences from `v1.2.1_src_only/behaviors` are documented as intentional current-branch adaptations, branch-specific gameplay choices, safety guards, or formatting-only differences.
- **Task N structures:** treated as complete because the reviewer asked this pass to assume Task N was done; this pass did **not** independently repeat the binary visual/NBT structure review.
- **In-game validation:** not proven by this file. Several resolved items may still deserve gameplay smoke-testing, but they are no longer open audit/porting decisions.

In short: **yes, the audit queue is complete; no, that does not mean every behaviour has been exhaustively in-game tested by this pass.**

## Recheck basis

- Rechecked the audit entries that were still listed after the earlier batches, excluding the Task N `.mcstructure` visual/NBT review from local verification.
- Compared the listed files under `v1.2.1_src_only/behaviors` with their active counterparts under `BOB_behaviors` by exact relative path and byte content.
- Non-identical files are not automatically unresolved: several are deliberate current-branch adaptations that preserve API compatibility, performance work, BOB-specific gameplay hooks, or safety guards.
- Result: **0 non-structure audit decisions remain open**. With Task N assumed complete, **0 behaviour-pack audit decisions remain open**. This does not claim full in-game verification.

## Remaining decisions by subsystem

| Area | Files still needing audit decision | Current status |
| --- | ---: | --- |
| Scripts | 0 | Completed: exact ports, current-branch API/performance adaptations, or documented intentional retentions. |
| Entities | 0 | Completed: vanilla overrides, boss/complex-mob behaviours, backpack entities, Nether/End mobs, and block-entity support reviewed. |
| Blocks | 0 | Completed: pale tall-plant custom-component/double-plant behaviour reviewed. |
| Trading | 0 | Completed: goblin trade table retained by design; no economy port needed. |
| Structures | 0 | Assumed completed as Task N per reviewer instruction; not revalidated locally in this pass. |
| Source-only | 0 | Completed: no unresolved source-only behaviour decisions remain. |

## Source-only decisions

No unresolved source-only behaviour files remain.

| Source-only file | Resolution | Reason |
| --- | --- | --- |
| `entities/vanilla/enderman.json` | Ported to `BOB_behaviors/entities/vanilla/enderman.json` with the source-only fixes. | The global Enderman override is approved for current main. Teleporting remains scoped to the not-riding component group, dragon-spawned Endermen can use `better_on_bedrock:dragon_summoned` to suppress loot/XP, the Ender Dragon summon applies that event, and bounty progress ignores the marked dragon-spawned variant. |
| `entities/vanilla/shulker.json` | Cherry-picked the shulker-armour targeting grouping into `BOB_behaviors/entities/end_mobs/shulker.json`. | Current main keeps its BOB-specific target priority, radius, persistence, reevaluation, and separate container-open targeting behaviour while adding the source-only armour targeting behaviour. |
| `items/ingots/stardust/stardust_block.json` | Do **not** port. | Current main's `blocks/other/ores/stardust_block.json` already declares `better_on_bedrock:stardust_block` with a construction creative-menu category, so it has the expected block-backed item form. The stardust-block recipe outputs that same identifier, and the ingot-from-block recipe consumes it back into 9 `better_on_bedrock:stardust_ingot`. Adding the v1.2.1 same-identifier `minecraft:item` block placer would be redundant and risks an identifier conflict. |

- Stardust tool item files now use flattened `pog:tool_durability` component entries because `minecraft:custom_components` is rejected as deprecated on Bedrock 1.26.21.1.


## Final recheck findings for previously listed non-structure items

### Blocks and block entity

| File | Status | Recheck finding |
| --- | --- | --- |
| `blocks/other/pale/tall_blossom.block.json` | Resolved | Retains current-branch base-level `pog:open_blossom` and `content:turn_to_air` registration so both halves participate in the double-plant cleanup path; the remaining diff from v1.2.1 is intentional. |
| `blocks/other/pale/tall_eyeblossom.block.json` | Resolved | Same reviewed pattern as tall blossom; custom-component placement was kept at the base component level rather than only in the default-state permutation. |
| `entities/blocks/tall_blossom.json` | Resolved | Functionally matches v1.2.1; the only remaining byte difference is trailing newline formatting. |

### Boss, End, Nether, and complex mob entities

| File | Status | Recheck finding |
| --- | --- | --- |
| `entities/bosses/inferior/inferior.behavior.json` | Resolved | Partially cherry-picked: kept `bob:inferior_active` for the cached script scan and corrected stale component-group removals while preserving the reviewed v1.2.1-compatible behaviour contract. |
| `entities/bosses/player_dummy_seeker.behavior.json` | Resolved | Retained current `bob:seeker_dummy_active` script-filter tag; no v1.2.1 gameplay fix outweighed that contract. |
| `entities/bosses/seeker/seeker.behavior.json` | Resolved | Retained current `bob:seeker_active` tag required by the Seeker scripts while keeping the reviewed component/event contract. |
| `entities/bosses/seeker/seeker_launcher.behavior.json` | Resolved | Retained current launcher active tag and matching intentionally misspelt internal timer/event names while preserving the reviewed particle/despawn behaviour. |
| `entities/bosses/willager/willager_clone.behavior.json` | Resolved | Partially cherry-picked: v1.2.1 clone scale and valid `minecraft:silverfish` summon are present; current-main broader target set is intentionally retained. |
| `entities/end_mobs/seeker.behavior.json` | Resolved | Retained current active tag, slower timers, and narrowed projectile cleanup because the v1.2.1 broad nearby-entity cleanup is less safe. |
| `entities/end_mobs/soot_collector.behavior.json` | Resolved | Retained current scoped summon commands, bounded rider selection, helper cleanup, and slower timers instead of the less-scoped v1.2.1 command paths. |
| `entities/nether_mobs/bassalt_crobber.behavior.json` | Resolved | Retained current target priorities, incomplete-path melee behaviour, and navigation damage-block avoidance as intentional current-main behaviour. |
| `entities/nether_mobs/fire_wisp.behavior.json` | Resolved | Retained requested current-main scale and custom hit-test box; no remaining event/component/loot mismatch was found. |
| `entities/nether_mobs/piglin.json` | Resolved | Retained current anger broadcasts to both `piglin` and `piglin_brute`, matching the intent that both should receive broadcasts. |

### Vanilla entity overrides

| File | Status | Recheck finding |
| --- | --- | --- |
| `entities/vanilla/bee.json` | Resolved | Partially cherry-picked: v1.2.1 baseline is present with deliberate BOB custom flower/wild-carrot feed and breed compatibility retained. |
| `entities/vanilla/chicken.json` | Resolved | Exact v1.2.1-byte match after porting. |
| `entities/vanilla/cow.json` | Resolved | Exact v1.2.1-byte match after porting. |
| `entities/vanilla/husk.json` | Resolved | Exact v1.2.1-byte match after porting. |
| `entities/vanilla/ocelot.json` | Resolved | Exact v1.2.1-byte match after porting. |
| `entities/vanilla/pig.json` | Resolved | Exact v1.2.1-byte match after porting. |
| `entities/vanilla/polar_bear.json` | Resolved | Exact v1.2.1-byte match after porting. |
| `entities/vanilla/rabbit.json` | Resolved | Exact v1.2.1-byte match after porting. |

### Script entries that were still listed

| File | Status | Recheck finding |
| --- | --- | --- |
| `scripts/commands/guidebook.js` | Resolved | Current branch keeps the v1.2.1 command registration intent while using safer player validity, inventory-container, and overflow handling. |
| `scripts/custom_components/content/blocks/turn_to_air.js` | Resolved | Current branch uses the modern `onPlayerDestroy` callback and removes the matching other half based on the destroyed `pog:double_plant` state. |
| `scripts/custom_components/kai/blocks/on_interact.js` | Resolved | Current branch retains the API-correct lowercase `GameMode.creative` check. |
| `scripts/custom_components/kai/blocks/on_player_destroy.js` | Resolved | Current branch uses the modern destroy callback, validates the player, and guards equipment access. |
| `scripts/custom_components/pog/blocks/custom_door.js` | Resolved | Current branch keeps optional-neighbour guards for placement, mirroring, linked-half updates, and tick cleanup. |
| `scripts/custom_components/pog/blocks/int.js` | Resolved | Current branch retains the API-compatible `onPlayerInteract` callback name. |
| `scripts/custom_components/pog/blocks/interact_placeholder.js` | Resolved | Current branch retains guarded player/equipment access and asynchronous loot/clear commands for the placeholder interaction path. |
| `scripts/custom_components/pog/blocks/small_logs.js` | Resolved | Current branch keeps optional-neighbour guards and API-compatible destroy handling. |
| `scripts/custom_components/pog/blocks/tick.js` | Resolved | Current branch preserves the v1.2.1 behaviours while retaining optional debug profiling and shared location reuse. |
| `scripts/custom_components/pog/blocks/ticking.js` | Resolved | Current branch validates the player, uses lowercase survival mode, and runs clear/loot commands asynchronously. |
| `scripts/custom_components/pog/items/end_podium.js` | Resolved | Current branch validates block/item/source state, uses async commands, and guards the delayed Poggy spawn against invalid blocks. |
| `scripts/functionality/armor_effects/amethyst.js` | Resolved | Current branch retains the existing knockback compatibility form used by this branch. |
| `scripts/functionality/blocks/end_blosson.js` | Resolved | Current branch adds air checks before creating the upward blossom base/head pair, avoiding overwrites. |
| `scripts/functionality/index.js` | Resolved | Current branch keeps explicit `.js` module specifiers required by the branch script style. |
| `scripts/functionality/items/addon-config.js` | Resolved | Current branch validates the player, guards client-system/inventory access, uses current modal-toggle arguments, and runs fog removal asynchronously. |

### Previously resolved script/entity subsystems retained as complete

| Subsystem | Status | Recheck finding |
| --- | --- | --- |
| Entity scripts: Inferior, Poggy, Seeker, Seeker teleport, Soot Eye | Resolved | Current-main helper-backed adaptations remain the correct resolution; script identifiers, tags, event names, and helper entities were cross-checked against active behaviour/resource references. |
| Backpack entities and `scripts/functionality/items/backpacks.js` | Resolved | Current-main robust storage architecture and the reviewed v1.2.1 `def` component-group shape are retained; no remaining code-audit follow-up is listed. |
| Item behaviour scripts: spear, ghost necklace, staffs, void boots, void totem, food effects, tool durability | Resolved | Current-main safety guards and API-compatible equipment/item access are retained; duplicate cooldown, durability, and item-consumption paths were checked. |
| Quest/WAWLA scripts | Resolved | Current-main dynamic-property parsing, batching, WAWLA display fixes, and v1.2.1-compatible storage/reward semantics are retained. |
| Enchantment and strip-block scripts | Resolved | Current-main queued mining, durability reservation, block revalidation, explicit strip-block mappings, and mutation guards are retained. |
| Ambience, fog, jukebox, enchanted bridge, and startup scripts | Resolved | Current-main high/low cadence scheduling, startup ordering, custom command/component registration, and v1.2.1-compatible jukebox/enchantment fixes are retained. |

## Task N structure assumption

Task N is treated as complete because the reviewer instructed this audit pass to assume it was done. The six previously listed `.mcstructure` files are therefore no longer listed as remaining work here, but this local pass did not repeat the binary visual/NBT comparison.

## Remaining changed same-path files needing audit decision

None. Gameplay smoke-testing can still be tracked separately if desired.
